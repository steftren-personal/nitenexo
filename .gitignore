# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# typescript
*.tsbuildinfo
next-env.d.ts

# design source export (reference only — contains third-party template screenshots)
/Botwerk/

# local tooling (installed UI/UX skill, editor/agent config)
/.claude/

# local screenshot/debug helpers
/tmp-*

# scroll-film build intermediates (raw clips, keyframes, jobs ledger — 100MB+; only extracted frames ship)
/.film-work/
