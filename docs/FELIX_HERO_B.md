Du bist Felix, Builder bei NiteNexo Solutions. Projekt: /root/projects/nitenexo. **Fortsetzung von docs/FELIX_HERO.md** (komplett lesen). Der vorige Durchlauf ist nach 28 Min am Modell-Limit abgebrochen. Der Arbeitsstand liegt UNCOMMITTET im Arbeitsverzeichnis (21 Dateien): `components/screens/Hero.tsx` (neu), `components/motion/HomeMotion.tsx` (neu), `app/home.css` (neu), `public/assets/hero/` (Videos/Poster kopiert), `ThreadFilm.tsx` + `splitChars.ts` gelöscht, HomeScreen/StoryBeat/ServicesBento/UseCaseTabs/CookieBanner/Footer/globals.css angepasst, `lib/homepage.browser.test.mjs` erweitert (RED-Commit 29041e9 existiert lokal). **Build ist grün** (First Load / = 233 kB, shared 102 kB).

## Vorgehen
1. `git status`, `git diff --stat`, dann die neuen/geänderten Dateien lesen. Prüfen, was von FELIX_HERO.md fehlt: vermutlich Mobile-Abnahme je Section (390/430 px), Faden-Linie (SVG scrub) am linken Rand, Cookie-Bottom-Sheet, Reduced-Motion/Save-Data-Gate fürs Video, Genjutsu-Audit, Screenshots, Commit.
2. Fehlendes fertigstellen. Nicht neu anfangen, nicht refactoren.
3. **Bundle prüfen**: First Load JS der Startseite war vor dem Umbau ca. 128-233 kB; Ziel: nicht größer als der Stand vor dem Umbau (`git stash` NICHT verwenden; vergleiche mit der Ausgabe im Report von Commit 75040f0, falls nicht auffindbar: Ziel ≤ 233 kB und im Report begründen).
4. Genjutsu design-audit (`/root/.hermes/skills/creative/genjutsu/references/design-audit.md`) abhaken.
5. Screenshots nach /tmp/shots/: `nn2-d-hero.png` (1440x900), `nn2-d-mid.png`, `nn2-m-hero.png` (390x844), `nn2-m-kapitel1.png`, `nn2-m-projekte.png`, `nn2-m-cta.png`. Kein leerer Viewport in einer Section.
6. Build + Lint + bestehende Tests grün. **Früh committen**: erst ein Zwischen-Commit mit grünem Build (`git -c user.name=Felix -c user.email=felix@nitenexo.at -m "feat(hero): KI-Loop-Hero, ThreadFilm entfernt, Section-Choreografie (WIP-sicher)"`), dann weiterarbeiten, am Ende Final-Commit `"feat(hero): Mobile-Abnahme, Faden-Linie, Audit"`. `git pull --rebase origin main` vor dem Push; `git push origin main` nach JEDEM Commit (Deploy läuft automatisch; ein grüner Zwischenstand darf live gehen).
7. VERIFICATION REPORT + Bundle-Delta + Liste der geprüften Mobile-Sections.
